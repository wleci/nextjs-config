# =============================================================================
# WLECI NEXT.JS FULL-STACK TEMPLATE - ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains environment variables for local development.
# Copy this file to .env.local for production or create .env.production
#
# SECURITY WARNING: Never commit sensitive production values to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# Base URL for your application (used for SEO, emails, redirects)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# SQLite (Development) - Local file database
DATABASE_URL="file:./dev.db"

# PostgreSQL (Production) - Uncomment and configure for production
# DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# MySQL (Alternative) - Uncomment if using MySQL
# DATABASE_URL="mysql://username:password@localhost:3306/database_name"

# -----------------------------------------------------------------------------
# AUTHENTICATION (NextAuth.js v5)
# -----------------------------------------------------------------------------
# Secret key for JWT signing and encryption (REQUIRED)
# Generate a secure key: openssl rand -base64 32
# PRODUCTION: Use a strong, unique secret (minimum 32 characters)
NEXTAUTH_SECRET="wleci-dev-secret-key-change-in-production-min-32-chars"

# Application URL for NextAuth.js callbacks
# Must match your actual domain in production
NEXTAUTH_URL="http://localhost:3000"

# Trust host header (required for some deployment platforms)
AUTH_TRUST_HOST="true"

# SOOON
# -----------------------------------------------------------------------------
# OPTIONAL: EMAIL CONFIGURATION (for future email features)
# -----------------------------------------------------------------------------
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# EMAIL_FROM="noreply@your-domain.com"

# -----------------------------------------------------------------------------
# OPTIONAL: EXTERNAL SERVICES
# -----------------------------------------------------------------------------
# UPLOADTHING_SECRET=""
# UPLOADTHING_APP_ID=""
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS
# -----------------------------------------------------------------------------
# Enable debug logging in development
# NODE_ENV="development"
# DEBUG="true"

# Database logging (uncomment to enable)
# DATABASE_LOGGING="true"
